# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Local secrets/config. `.env.local` is already covered by *.local, but a plain
# `.env` is not — and it's the one people create by habit.
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Raw source media dumped in repo root (originals; real assets live in public/ & src/)
/*.png
/*.jpg
/*.jpeg
/*.webp
/*.avif
/*.mp4
/*.zip
/*.htm
